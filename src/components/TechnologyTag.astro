---
import Image from "astro/components/Image.astro";

interface Props {
  name: string;
  svg: string;
  className: string;
}

const { name, svg, className } = Astro.props;
---

<div
  class:list={[
    "flex flex-row items-center justify-center gap-2 rounded-lg border border-gray-200 p-2 transition-all hover:shadow-sm",
    className,
  ]}
>
  <Image width={24} height={24} src={`/${svg}.svg`} alt={`${name} icon`} />
  <p>{name}</p>
</div>
